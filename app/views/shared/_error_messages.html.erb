<% if target.errors.any? %>
  <div class="error_explanation">
    <p><strong><%= pluralize(target.errors.count, "error") %> prevented this record from being saved:</strong></p>
    <ul>
      <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
